# spring

`spring()` creates physically based motion tied to the current frame and fps.

Use it for subtle scale-in or text entrance effects rather than extreme bouncing in narrative edits.

Pass `frame` and `fps`, and optionally damping or mass values to tune the feel.

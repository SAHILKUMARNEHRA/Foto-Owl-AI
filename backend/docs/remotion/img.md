# Img

`Img` renders static images in Remotion compositions.

Use regular `style` props with `objectFit: "cover"` for full-bleed imagery.

Prefer stable absolute or project-relative paths that are available at render time.

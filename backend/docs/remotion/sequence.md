# Sequence

`Sequence` offsets a block of content on the Remotion timeline.

Use `from` in frames to control when a scene starts and `durationInFrames` to bound how long it stays visible.

Nested sequences are allowed, but keeping one sequence per scene simplifies compilation and debugging.

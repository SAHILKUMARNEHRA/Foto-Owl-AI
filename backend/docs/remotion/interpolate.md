# interpolate

`interpolate()` maps frame progress to animated values.

Always clamp ranges when the output should stop growing or shrinking outside the intended interval.

It is commonly used for scale, translate, opacity, and blur animations.

# AbsoluteFill

`AbsoluteFill` is a full-screen positioned container from Remotion.

Use it as the root wrapper for each composition or scene layer when you want predictable layout behavior.

It accepts standard React style props and children.
